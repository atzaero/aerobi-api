/** Limite máximo de itens por página em `GET /contact`. */
export const CONTACT_LIST_MAX_LIMIT = 100;

/** SHA-256 hex (64 chars) — mesmo formato gerado pelo `aerobi-web`. */
export const CONTACT_SESSION_HASH_REGEX = /^[a-f0-9]{64}$/i;
